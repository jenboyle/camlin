name: Run Jest Tests

on:
  push:
    branches:
      - main

jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run Tests
        run: |
          npm run build 
          npm run test
